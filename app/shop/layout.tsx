export const metadata = {
  title: 'Shop Custom Packaging | Boxes, Bags, Cups & Containers',
  description: 'Browse custom packaging solutions for hospitality and retail. Low MOQs, fast lead times, FDA-approved food-safe options. Custom boxes, bags, cups, and containers with branded printing.',
};

export default function ShopLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
